query GetProducts {
  products(first: 100, channel: "default-channel") {
    totalCount
    edges {
      node {
        id
        name
        slug
        updatedAt
        channel
        media {
          url
          id
        }
      }
    }
  }
}
